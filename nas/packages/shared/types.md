# Shared Types

```ts
type RemoteDraft = {
  text: string;
};

type HistoryEntry = {
  title: string;
  path: string;
  modifiedAt: string;
  hasGamePreview: boolean;
  hasFinalReport: boolean;
};
```
